pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "caraprojetada"

include ":app"
// droidVNC-NG embutido como submódulo
include ':droidvnc'
project(':droidvnc').projectDir = new File(rootDir, 'droidvnc')
